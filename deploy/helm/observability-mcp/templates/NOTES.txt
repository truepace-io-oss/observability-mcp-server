observability-mcp {{ .Chart.AppVersion }} deployed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

The MCP endpoint is served at path /mcp on:
  Service: {{ include "observability-mcp.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }}
{{- if .Values.ingress.enabled }}
  Ingress: https://{{ .Values.ingress.host }}/mcp   (class: {{ .Values.ingress.className }})
{{- end }}

Configured datasources:
{{- if .Values.datasources }}
{{- range .Values.datasources }}
  - {{ .name }} ({{ .type }} @ {{ .url }})
{{- end }}
{{- else }}
  (none — set .Values.datasources to register observability backends)
{{- end }}

⚠️  The streamable-HTTP transport has NO built-in authentication. Only expose it
    on a trusted / internal ingress (e.g. WireGuard). Never expose it publicly.

Add it to an AI agent (Claude Code) with:
{{- if .Values.ingress.enabled }}
  claude mcp add --transport http {{ .Release.Name }} https://{{ .Values.ingress.host }}/mcp
{{- else }}
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "observability-mcp.fullname" . }} 9090:{{ .Values.service.port }}
  claude mcp add --transport http {{ .Release.Name }} http://127.0.0.1:9090/mcp
{{- end }}
